apiVersion: apps/v1
kind: Deployment
metadata:
  name: devapp2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devapp2
  template:
    metadata:
      labels:
        app: devapp2
    spec:
      containers:
        - name: nginx
          image: nginx
